package api_test

import (
	"testing"
)

// /// Cross origin is not allowed
func TestChatGetGroupSticker(t *testing.T) {
	// apiSession, saveSession := scenario.GetTokopediaApiClient()
	// defer saveSession()

	// variable := model_public.TypeVar{
	// 	Type: 1,
	// }
	// hasil, err := apiSession.ChatGetGroupSticker(&variable)
	// assert.Nil(t, err)
	// assert.NotEmpty(t, hasil)
}

// /// Cross origin is not allowed
func TestChatGetBundleSticker(t *testing.T) {
	// apiSession, saveSession := scenario.GetTokopediaApiClient()
	// defer saveSession()

	// variable := model_public.ChatGetBundleStickerVar{
	// 	ID:    "f0991c80-8e77-11ea-bb5e-000000000000",
	// 	Limit: 8,
	// 	MaxID: nil,
	// }

	// hasil, err := apiSession.ChatGetBundleSticker(&variable)
	// assert.Nil(t, err)
	// assert.NotEmpty(t, hasil)
}
